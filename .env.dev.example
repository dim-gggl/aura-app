# =============================================================================
# AURA ART - DEVELOPMENT ENVIRONMENT VARIABLES
# =============================================================================
# Copy this to .env for local development
# =============================================================================

# IMPORTANT: For local development, use dev settings
DJANGO_SETTINGS_MODULE=aura_app.settings.dev

# Debug mode (should be True for development)
DEBUG=True

# Allowed hosts for development
ALLOWED_HOSTS=127.0.0.1,localhost

# Secret key for development (generate a new one for production!)
SECRET_KEY=dev-secret-key-change-this-in-production

# Database (SQLite for development, PostgreSQL for production)
# Comment out DATABASE_URL to use SQLite in development
# DATABASE_URL=postgresql://user:password@localhost:5432/aura_dev

# Media and Static files
MEDIA_URL=/media/
STATIC_URL=/static/

# CSRF Settings (for development)
CSRF_TRUSTED_ORIGINS=http://127.0.0.1:8000,http://localhost:8000

# Optional: Redis for caching (comment out if not using)
# REDIS_URL=redis://localhost:6379/0
